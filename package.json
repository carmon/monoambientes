{
  "name": "monoambientes2025",
  "version": "0.1.0",
  "scripts": {
    "deploy": "npm run generate-index",
    "local-dev": "npx http-server public",
    "compile-libs": "tsc --p tsconfig.json",
    "generate-index": "node --experimental-strip-types src/0_preload/generate-index.ts"
  },
  "dependencies": {
    "mongodb": "^6.12.0"
  },
  "devDependencies": {
    "@types/google.maps": "^3.58.1",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.3"
  }
}
