{
  "name": "monoambientes2025",
  "version": "0.1.0",
  "scripts": {
    "deploy": "npm run build",
    "local-dev": "npx http-server public",
    "build": "node --experimental-strip-types src/build/generate-index.ts"
  },
  "dependencies": {
    "mongodb": "^6.12.0"
  },
  "devDependencies": {
    "@types/google.maps": "^3.58.1",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.3"
  }
}
